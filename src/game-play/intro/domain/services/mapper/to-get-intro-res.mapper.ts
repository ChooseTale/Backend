import { GetIntroScreenResDto } from '@@src/game-play/intro/applications/dto/get-intro-screnn.dto';
import { IntroEntity } from '../../entities/intro-data.entity';

export const toGetIntroRes = (
  introEntity: IntroEntity,
): GetIntroScreenResDto => {
  return {} as any;
};
